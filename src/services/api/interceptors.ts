/**
 * No refresh logic — 401 handling is done in the client.
 * When your CTO adds a refresh API, you can add interceptors here
 * (e.g. retry with refresh token before calling onUnauthorized).
 */

export {};
